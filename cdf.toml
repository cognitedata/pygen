[cdf]
default_organization_dir = "cdf"
default_env = "dev"

[modules]
# This is the version of the modules. It should not be changed manually.
# It will be updated by the 'cdf module upgrade' command.
version = "0.5.16"

[plugins]
run = false
pull = false
dump = true

[alpha_flags]
graphql = true
purge = true
